using System;

class Program
{
    static void Main()
    {
        char[] alphArray = new char[26];
        for (int i = 0; i < 26; i++)
        {
            alphArray[i] = (char)(97 + i);
        }

        foreach (char letter in alphArray)
        {
            Console.WriteLine(letter);
        }
    }
}
